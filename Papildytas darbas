include <iostream>
using namespace std;


int main(void)
{
    int x = 0, sum = 0, total = 0;
    double grit = 0.0;

    total++;
    cout << "New ideas and projects spmetimes distract me from previous ones" << endl;
    cout << "Not at all - 5; Not much - 4; Somewhat - 3; Mostly - 2; Very much - 1:";

    cin >> x;
    sum +=x;

    grit = sum / total;
    cout << "Your Grit score is" << grit << endl;
    return 0;
}

{
    int x = 0, sum = 0, total = 0;
    double grit = 0.0;

    total++;
    cout << "Set backs don't discourage me. I don't give up easily." << endl;
    cout << "Not at all - 5; Not much - 4; Somewhat - 3; Mostly - 2; Very much - 1:";

    cin >> x;
    sum +=;

    grit = sum / total;
    cout << "Your Grit score is" << grit << endl;
    return 0;
}

{
    int x = 0, sum = 0, total = 0;
    double grit = 0.0;

    total++;
    cout << "I often choose a goal but later pursue a different one." << endl;
    cout << "Not at all - 5; Not much - 4; Somewhat - 3; Mostly - 2; Very much - 1:";

    cin >> x;
    sum +=;

    grit = sum / total;
    cout << "Your Grit score is" << grit << endl;
    return 0;
}

{
    int x = 0, sum = 0, total + 0;
    double grit + 0.0;

    total++;
    cout << "I am a hard worker" << endl;
    cout << "Not at all - 5; Not much - 4; Somewhat - 3; Mostly - 2; Very much - 1:";

    cin >> x;
    sum =+;

    grit = sum / total
    cout << "Your Grit Score is" << grit << endl;
    return 0;
}

{
    int x = 0, sum = 0, total + 0;
    double grit = 0.0;

    total++;
    cout << "I have a difficulty maintaining my focus on projects that take. more than few months to complete" << endl;
    cout << "Not at all - 5; Not much - 4; Somewhat - 3; Mostly - 2; Very much - 1:";

    cin >> x;
    sum =+;

    grit = sum / total
    cout << "Your Grit Score is" << grit << endl;
    return 0;
}

{
    int x = 0, sum = 0, total = 0;
    double grit = 0.0;

    total++;
    cout <<"I finish whatever i begin" << endl;
    cout << "Not at all - 5; Not much - 4; Somewhat - 3; Mostly - 2; Very much - 1:";

    cin >> x;
    sum =+;

    grit = sum / total
    cout << "Your Grit Score is" << grit << endl;
    return 0;
}

{
    int x = 0, sum = 0, total = 0;
    double grit = 0.0;

    total++;
    cout <<"My interest change form year to year" << endl;
    cout << "Not at all - 5; Not much - 4; Somewhat - 3; Mostly - 2; Very much - 1:";

    cin >> x;
    sum =+;

    grit = sum / total
    cout << "Your Grit Score is" << grit << endl;
    return 0;
}

{
    int x = 0, sum = 0, total = 0;
    double grit = 0.0;

    total++;
    cout << "I am diligent. I never give up." << endl;
    cout << "Not at all - 5; Not much - 4; Somewhat - 3; Mostly - 2; Very much - 1:";

    cin >> x;
    sum =+;

    grit = sum / total
    cout << "Your Grit Score is" << grit << endl;
    return 0;
}

{
    int x = 0, sum = 0, total = 0;
    double grit = 0.0;

    total++;
    cout <<"I have been obsessed wirh a certain idea or project for a short time but i lost ineterest" << endl;
    cout << "Not at all - 5; Not much - 4; Somewhat - 3; Mostly - 2; Very much - 1:";

    cin >> x;
    sum =+;

    grit = sum / total
    cout << "Your Grit Score is" << grit << endl;
    return 0;
}

{
    int x = 0, sum = 0, total = 0;
    double grit = 0.0;

    total++;
    cout << "I have overcome setbacks and conquer an important challange" << endl;
    cout << "Not at all - 5; Not much - 4; Somewhat - 3; Mostly - 2; Very much - 1:";

    cin >> x;
    sum =+;

    grit = sum / total
    cout << "Your Grit Score is" << grit << endl;
    return 0;
}
